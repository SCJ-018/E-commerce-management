# 电商后台管理系统 — 技术解析与现状评估

> 生成日期：2026-09-07
> 依据：对全量代码的精读（backend/app.py 5914 行、js/app.js 6171 行、index.html 2346 行、database/*.sql、tools/*.py、.deploy/*）

---

## 0. 一句话结论（先看这个）

这是一个给「聚浪网络 JULANG NETWORK」用的**多平台电商经营后台**，主体已经上线能跑（腾讯云 119.45.187.154，域名 julangkeji.site）。核心价值是把抖店 / 京东 / 千牛（淘宝）/ 天猫 / 1688 / 抖音 / 小红书的数据集中到一个后台，做营销分析、订单明细、选品、种草监测，并用 DeepSeek AI 生成每日经营分析报告。

但系统有明显的"**两头重、中间乱**"特征：

- 后端 `app.py` 是 5914 行的"上帝文件"，前端 `app.js` 是 6171 行的"上帝文件"，全部手写 SQL / 原生 JS，无分层、无测试、无类型。
- 15 个页面里 **3 个还是假数据占位**（店铺账号、运营业绩、财务中心），其余接真实数据。
- **爬虫都是"一次性快照脚本"，不能长久无人值守**（见第 6 节）。
- 你遇到的"影刀导入、昨天数据没更新"，根子大概率在**导入环节的日期/字段匹配**，不是系统挂了（见第 7 节，需你配合确认几个点才能定死）。

---

## 1. 系统定位与部署现状

| 项 | 内容 |
|---|---|
| 品牌 | 聚浪网络 JULANG NETWORK |
| 用途 | 多平台电商经营数据聚合 + 分析 + 选品 + 种草监测后台 |
| 服务器 | 腾讯云 119.45.187.154（南京，TencentOS 3.3，4核8G，50G系统盘 + 20G数据盘） |
| 代码位置 | `/opt/ecom`（backend/ + index.html + css/ + js/ + tools/category_mapper/ + tools/seeding_accounts.json） |
| 运行方式 | gunicorn 单 worker 8 线程，监听 0.0.0.0:5000；systemd 服务名 `ecom` |
| 反向代理 | nginx 80/443 → 127.0.0.1:5000；HTTPS 用 Let's Encrypt（证书 2026-11-29 到期，已配自动续期 timer） |
| 数据库（线上站点专用） | 服务器本机 MySQL 8.0.46（127.0.0.1:3306），库名「数据」（18 张表，从内网 192.168.2.10 迁移）。**本地所有入口不连它** |
| 域名 | julangkeji.site（已 ICP 备案） |
| **统一数据源（本地全部入口）** | **192.168.2.10:3306（内网自建 MySQL，DB 名「数据」）** — 后端 API、品类映射脚本、导入工具统一指向此处（2026-09-14 定案） |

**登录鉴权**：`app.py` 用 cookie 会话鉴权，`/api/auth/login` 下发 HttpOnly token，`before_request` 校验所有 `/api/*`（白名单 login/logout/health）。token 内存存储（单 worker），24h 滑动续期。
⚠️ 账号密码**明文**存 `admin_accounts` 表，默认 `admin` 账号弱口令 `admin123`（需改）。

---

## 2. 技术栈

| 层 | 技术 | 说明 |
|---|---|---|
| 后端 | Python 3.12 + Flask | 单文件 `app.py`，自建 MySQL 连接池 |
| 数据库访问 | PyMySQL，手写 SQL | 无 ORM，表名/字段名大量是中文 |
| 前端 | 原生 HTML/CSS/JS + ECharts | 无框架、无构建、无打包，单文件 `app.js` |
| AI | DeepSeek API | 全局 `deepseek-chat`；种草智能体 `deepseek-v4-flash`；选品用独立 key |
| OCR | PaddleOCR + OpenCV | 违规词检测里的图片文字识别 |
| 爬虫 | requests / curl_cffi / Playwright / PyExecJS | 见第 6 节 |
| 部署 | gunicorn + nginx + systemd + certbot | 单进程 |
| 运维辅助 | `.deploy/ssx.py`（SSH 自动化）、hsk-cli（内网穿透/托管） | 见 `.claude/skills/` |

---

## 3. 架构特点（决定后面怎么改）

1. **单体 + 单进程**：Flask 一个进程既 serve API，又直接返回静态页面（`/` 返回 index.html，`/css`、`/js` 返回静态文件）。没有前后端分离构建。

2. **两个"上帝文件"**：
   - `backend/app.py`（5914 行）：所有 API 路由、DeepSeek 调用、爬虫调度、OCR、自动建表，全部按"功能注释段"（`# ====== XXX ======`）堆在一个文件里。
   - `js/app.js`（6171 行）：一个 IIFE 模块 `App`（内含 `ApiService`），所有 15 个页面的渲染逻辑都在里面。

3. **中文表名/字段名**：`店铺营销数据`、`成交金额`、`统计周期`… 好处是业务人员能看懂；坏处是任何一处写错一个字就查不到数据，且跨工具兼容性差（Windows 下 mysqldump 中文库名会乱码，迁移时已经踩过坑）。

4. **配置分散且部分硬编码**：
   - DB 配置集中在 `backend/config.py`（唯一真源），指向自建 MySQL `192.168.2.10:3306`。
   - **敏感信息（DB 密码、DeepSeek Key）已移出代码**：改由项目根目录 `.env` 提供（模板 `.env.example`，已提交）。优先级：系统环境变量 > `.env` > config.py 兜底值。`.env` 与 `config.py` 均被 .gitignore 排除；已确认 `config.py` 从未进入 git 历史。启动时若凭据缺失或误配到腾讯云，config.py 会打印醒目告警（正常配置时静默）。
   - 导入桌面工具 `tools/import_tool/import_tool.py` 已改为优先复用 `backend/config.py` 的 DB_CONFIG（取不到时回退内置的 `192.168.2.10:3306`），不再走 SSH 隧道 127.0.0.1:3307 连服务器库。

---

## 4. 数据模型（理解系统的钥匙）

系统有**两套完全不同的业务数据域**，这是排查"昨天数据没更新"必须分清的前提。

### 域 A：店铺营销域（大盘数据）
- 核心表：`店铺营销数据`（字段：`日期`、`平台`、`品牌`、`店铺名`、`净支付金额`、`支付金额`、`退款金额`、`推广花费`、`推广总成交`、`访客数`、`支付买家数`、`支付转化率`、`订单退款率`、`客单价`…）
- 消费它的页面：**整体营销总览、品类营销、分平台/店铺、每日数据分析、数据看板首页**
- ⚠️ **这张表的建表 SQL 不在仓库里**。仓库里只有 `database/schema.sql`（3 张 demo 表）和 `database/单链接数据表.sql`（3 张单链接表）。`店铺营销数据`、`千牛单链接推广数据表`、`爱搜数据表`、`天猫榜单表` 等都是内网库现成迁移来的，**结构没有留存文档**。

### 域 B：单链接订单域（商品级数据）
- `抖店单链接数据表`（主键：`统计周期` + `商品编码`）
- `京东单链接数据表`（主键：`时间` + `SPU`）
- `千牛单链接数据表`（主键：`统计日期` + `商品ID`）
- `千牛单链接推广数据表`（推广消耗、成交等，按 `商品ID` 关联）
- 消费它的页面：**订单详情**

### 域 C：选品/工具域
- `天猫榜单表`、`抖音热搜品类表`、`爱搜数据表`、`商品品类映射表`、`选品记录表`

### 域 D：管理域
- `admin_accounts`、`admin_roles`（权限）、`员工花名册`、`daily_analysis_reports`（AI 报告缓存）
- demo 残留：`products` / `orders` / `customers`（`schema.sql` 里是示例数据；通用 DB 浏览器 `/api/db/tables` 只管理这 3 张）

### 域 E：种草域（不是数据库，是本地文件）
- 抖音作品 `tools/_douyin_works.csv`、小红书作品 `tools/_xhs_works.json`、被删作品状态 `tools/_seeding_state.json`、账号 `tools/seeding_accounts.json`

---

## 5. 15 个页面模块逐个现状

| # | 页面 | 数据源 | 状态 |
|---|---|---|---|
| 1 | 整体营销数据总览 | `店铺营销数据` | ✅ 真实 |
| 2 | 品类营销数据 | 单链接表 + `商品品类映射表` | ✅ 真实 |
| 3 | 分平台/店铺详细数据 | `店铺营销数据` | ✅ 真实 |
| 4 | 订单详情 | 抖店/京东/千牛单链接表 | ✅ 真实 |
| 5 | 每日数据分析 | 店铺营销 + 单链接 + 推广 + 品类 + DeepSeek | ✅ 真实（AI 报告 + PDF 下载） |
| 6 | 店铺账号管理 | `getMockStoreAccounts()` | ❌ 假数据，增删改按钮全"开发中" |
| 7 | 运营业绩面板 | `getMockOpData()` 随机数 | ❌ 假数据 |
| 8 | 选品助手 | 天猫榜单/抖音热搜/爱搜/1688/选品智能体 | ✅ 真实（依赖爬虫 + AI） |
| 9 | 种草监测中台 | CSV/JSON 文件 + 爬虫 + 种草AI | ⚠️ 半成品（mock 与真爬虫混合） |
| 10 | 财务中心 | `getMockFinanceData()` 随机数 | ❌ 假数据 |
| 11 | 人事中心 | `员工花名册` | ✅ 真实 |
| 12 | 数据导入 | 上传 Excel → 5 张业务表 | ✅ 真实（有坑，见第 7 节） |
| 13 | 违规词检测 | PaddleOCR + 违规词库 | ✅ 真实 |
| 14 | 管理员与权限 | `admin_accounts` / `admin_roles` | ✅ 真实（RBAC） |
| 15 | 个人中心设置 | — | ✅ 真实 |

> 说明：店铺账号、运营业绩、财务中心三个页面在前端直接调用 `getMock*()` 生成随机假数据，**后端没有任何对应 API**。这是"前端画了壳、后端没做"的占位页面。

---

## 6. 爬虫体系评估（能否长久使用）

`tools/` 下共 10 个采集脚本 + 2 个工具脚本 + 6 个 cookie 文件。技术栈分三类：

- **纯 requests 打未公开 web 接口**：`douyin_scraper.py`（部分）、`douyin_video_scraper.py`、`douyin_hot_scraper.py`
- **Playwright + Chrome persistent profile 抓 DOM**：`taobao_scraper.py`、`tmall_batch_scraper.py`、`1688_scraper.py`、`aisou_scraper.py`
- **逆向签名（curl_cffi + Node.js 签名 + 指纹）**：`xhs_crawler.py`、`seeding_xhs.py`

**共同致命伤**：所有脚本都是"单文件脚本 + 进度 JSON 轮询 + 落本地 JSON/CSV 不落库"，**没有任何定时调度、重试、监控、告警**；认证全部是人工从浏览器 F12 复制的一次性 cookie / localStorage / 硬编码指纹快照，无自动刷新机制。

### 逐脚本结论（按脆弱度排序）

| 脚本 | 采集什么 | 结论 |
|---|---|---|
| `xhs_crawler.py` / `seeding_xhs.py` | 小红书作品（点赞/收藏/评论/正文/IP） | 🔴 最脆。逆向签名（X-s/X-t/X-S-Common/a1/b1/MNS…）+ Node.js 运行时 + 依赖未纳入版本库的第三方 `Spider_XHS/`，签名算法一变即死，需逆向能力修复 |
| `douyin_video_scraper.py` | 抖音账号作品 | 🔴 随时挂。硬编码设备指纹（WEBID/MS_TOKEN/VERIFY_FP）+ 硬编码回退账号 + 未公开接口，cookie 失效直接 `sys.exit` 无重试 |
| `1688_scraper.py` | 1688 前 10 页货源 | 🔴 随时挂。1688 滑块风控最严，无头默认大概率被拦，只能靠 `1688_local_scrape.py` 人工拖滑块兜底 |
| `taobao_scraper.py` / `tmall_batch_scraper.py` | 淘宝/天猫销量榜 | 🟠 随时挂。硬编码 CSS class 选择器（`[class*='title--']` 等），淘宝前端改版即废；且 `SCRAPE_JS` 重复两份，改一处要改两处 |
| `aisou_scraper.py` | 爱搜关键词数据 | 🟠 脆弱。硬编码 el-table 列索引 + tab 文本 + localStorage JWT，改版即错位 |
| `douyin_hot_scraper.py` | 抖音热搜榜 | 🟠 随时挂，但影响面小（只是热搜词展示） |
| `douyin_scraper.py` | 抖音商城销量前 50 | ⚪ 半可用。默认数据源是 **mock 假数据**；真实数据要走付费第三方 OneBound API（字段映射未验证） |
| `category_mapper/mapper.py` | 商品 → 统一品类映射 | 🟢 相对稳健。有重试/增量/dry-run/LLM 失败落盘，且不依赖反爬（只依赖 DB + DeepSeek key） |

### 总体判断

**这些爬虫不能长久无人值守使用，本质是一次性快照脚本。** 原因是三个硬伤：

1. 认证全是人工复制的一次性 cookie/localStorage/指纹，抖音/淘宝/1688/小红的 cookie 通常几天到几周过期，过期即断。
2. 采集逻辑深度绑定对方页面结构（CSS class、el-table 列序、未公开接口字段名），对方一次改版或风控升级，脚本**静默失败或输出错位数据**——而且很多是"空结果静默跳过"而非报错（如 `tmall_batch_scraper.py` 的 `added==0 就 break`、`douyin_hot_scraper.py` 的 `except: words=[]`），你拿到的可能是**不完整的数据而不自知**。
3. 没有监控/告警/自愈，失败就写个 `error` 进度，等人工看到。

> 结论：它们的正确姿态是"**用到坏、坏了再人工修**"，不要指望"部署一次跑半年"。若要长期可用，至少要解决三件事：① 认证自动续期（而非人工复制 cookie）；② 对方页面结构变更的检测与告警；③ 把重复的抓取逻辑收敛到共享模块。

---

## 7. 影刀 RPA → Excel → 导入 → "昨天数据没更新" 排查

### 7.1 这条链路是怎么走的

```
影刀 RPA（上一任开发的，在你本地电脑跑）
  → 更新一份 Excel
  → 你在后台「数据导入」页上传这个 Excel
  → 后端 /api/import/excel（backend/app.py:2420）
  → 写 MySQL（抖店/京东/千牛单链接表、千牛推广表、店铺营销数据）
  → 「订单详情」「营销总览」等页面读库展示
```

### 7.2 系统里有两条导入路径，行为不一样（关键）

1. **网页「数据导入」页** → `/api/import/excel`：用 `INSERT ... ON DUPLICATE KEY UPDATE`（主键冲突就覆盖更新），这是正确的"重复导入会更新"逻辑。
2. **本地桌面工具** `tools/import_tool/import_tool.py`（tkinter GUI）：用纯 `INSERT` + 事后 `DELETE` 去重，主键重复会直接失败。

你说的提示"成功多少条、失败多少条"，与网页版的提示「导入完成：成功 X 行，失败 Y 行」完全对应，**所以你应该用的是网页版（路径 1）**。

### 7.3 为什么"失败 N 条" + "昨天数据没更新"——四个可能根因

> ✅ **已确认结论（2026-09-07，据用户实际操作）**
> 用户导入的是 **`抖店单链接数据表`**（成功 28065 行，失败 1729 行，文件约 29795 行），
> 但去看的是 **`整体营销数据总览`** —— 该页读的是 **`店铺营销数据`**（另一张独立的店铺级大盘表）。
> 两张表互不喂数据，所以"整体营销总览看不到刚导的抖店数据"是**表域错位**，不是导入失败。
> **正确查看位置：`订单详情` 页 → 平台选「抖店」→ 点「昨日」。**
> 剩下要解决的只有两件事：① 1729 行失败的具体原因（前端已加显示，见下）；② 确定 `店铺营销数据` 这张大盘表到底该由什么数据/工具来填。

#### 根因 1（最可能）：失败原因被前端吞掉了，你看不到真正的错误
后端 `import_excel` 确实会把第一条失败原因放进返回的 `error` 字段，但前端 `importExcel()`（`js/app.js:6081`）只显示 `json.msg`（"成功X失败Y"），**不显示 `json.error`**。

从代码推断，失败最常见的原因是：**三张单链接表的所有字段都是 `NOT NULL`**（见 `database/单链接数据表.sql`）。影刀导出的 Excel 只要缺了某列、或某些格子为空（被 `_import_convert` 转成 `None`），这一行就会报 `Column 'X' cannot be null` → 计为失败。

#### 根因 2：日期列没匹配上 / 格式不对
单链接表的日期列叫「统计周期」「时间」「统计日期」，导入时按**列名精确匹配**。如果影刀 Excel 里这个列名不完全一致，或日期格式不在已支持列表（`%Y-%m-%d` / `%Y/%m/%d` / `%Y%m%d` 等）里，日期就转失败或转成错值 → 你在订单详情点"昨日"就筛不到。

#### 根因 3：你导入的表 和 你看的页面 不是同一个数据域
- 影刀导的是「抖店单链接数据表」→ 必须去「**订单详情**」页看"昨日"。
- 如果你去看「**整体营销数据总览**」或「**每日数据分析**」，它们读的是「**店铺营销数据**」——另一张表，自然看不到刚导的数据。

#### 根因 4：影刀 Excel 里的日期，本来就不是"昨天"
- 抖店的"统计周期"可能是数据结算的截止日，不等于"今天的前一天"。若影刀导出的是 2026-09-05（前天），你在订单详情点"昨日"（2026-09-06）当然看不到。
- 附带一个前端小 bug：订单详情页的"昨日"用 `new Date(Date.now()-86400000).toISOString()`（`js/app.js:4202`），`toISOString()` 是 UTC 时间，北京时间每天 0:00~8:00 会偏一天（显示成"前天"）。

### 7.4 需要你配合做的几件事（这决定能不能定位死）

见文末第 9 节的问题清单（Q1~Q6）。

---

## 8. 后续怎么搞（建议路线图，分优先级）

### P0 —— 先解决眼前的"导入没更新"
1. **让前端把导入失败原因显示出来**（后端已经返回了 `error`，前端只差一行展示）。我可以直接改，2 行代码。
2. **补全"幽灵表"结构**：把 `店铺营销数据`、`千牛单链接推广数据表`、`爱搜数据表`、`天猫榜单表` 的真实结构导出来（`SHOW CREATE TABLE`），存档进仓库，否则任何排查都是盲人摸象。
3. **确认影刀 Excel 的列名和日期格式**是否与目标表一致（见第 9 节 Q4）。

### P1 —— 让系统更稳、更安全
4. 改掉 `admin/admin123` 弱口令；处理 DeepSeek key 已在 config.py 明文且进过 git 历史的问题（建议轮换 key）。
5. 把 DB 密码兜底 `123456` 也彻底移出代码（现在 config.py 里还有 fallback）。
6. 给爬虫加"空结果/异常告警"，避免"数据不完整而不自知"。

### P2 —— 爬虫治理（如果还要长期用）
7. 把重复的抓取逻辑（`SCRAPE_JS`、`parse_cookies`、`write_progress`）收敛成共享模块。
8. 认证续期自动化（cookie 过期检测 + 提醒重新扫码/复制），而不是坏了才发现。
9. 对 1688 / 小红书这两个最脆的，明确"是否还值得维护"，评估换成第三方 API 或干脆停用。

### P3 —— 架构演进（可选，现在不建议大动）
10. 若系统还要继续扩展，中长期应考虑拆分 `app.py`（按域拆模块）和 `app.js`（按页拆），引入数据库 schema 版本管理。但**当前阶段不建议**——系统能跑，收益不明确，风险高。

---

## 9. 需要你回答的问题清单（按重要性）

**Q1（最关键）**：在「数据导入」页，你选的目标表是哪一个？
- 抖店单链接数据表 / 京东 / 千牛 / 千牛单链接推广数据表 / 店铺营销数据？

**Q2**：你说"看昨天的数据没更新"，具体是在哪个页面看的？
- 「订单详情」的"昨日"？还是「整体营销数据总览」？还是「每日数据分析」？

**Q3**：导入后提示的"失败 N 条"，N 大概是多少？成功是多少？导入时选的目标表、上传的文件，能不能告诉我文件里大概有多少行？

**Q4**：打开影刀导出的那个 Excel，告诉我：
- 第一行表头（列名）都有哪些（尤其日期列叫什么、商品编码/ID 列叫什么）；
- 日期列里实际填的日期是几号、什么格式（例如 `2026-09-06` 还是 `2026/9/6` 还是别的）。

**Q5**：影刀这个 RPA 是抓的哪个平台的什么数据？（抖店？京东？千牛？）它导出的 Excel 是"只含昨天一天"，还是"累积多天"？

**Q6**：你用的是网页版「数据导入」，还是那个本地桌面工具 `import_tool.py`？（你描述"成功多少条失败多少条"更像网页版，但我需要确认。）

---

*以上。核心就一句：系统主体能用，爬虫不能长期无人值守，你眼前的问题大概率是"导入失败原因被藏起来了 + 日期/字段没对上"，把 Q1~Q6 答了，我就能帮你定位到具体哪一行出的问题。*
