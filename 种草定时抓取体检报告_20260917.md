# 种草监测中台 · 定时抓取数据更新功能体检报告

- **检查时间**：2026-09-17 13:41 ~ 13:55（CST）
- **检查对象**：`backend/app.py` 的 `_seeding_auto_update_loop` 定时线程 + `tools/douyin_video_scraper.py` + `tools/seeding_xhs.py` + 前端「数据更新」面板
- **检查方式**：本地代码走查 + 服务器实测（`/opt/ecom` 真实运行态、journalctl、进度文件、日志、Cookie 探测）
- **一键复检脚本**：`.deploy/_seed_check.py`（只读，不触发抓取、不改数据）

---

## 一、结论先行

> **定时线程本身在正常工作（每 30 分钟一次，今日 8 轮全部按时触发），但抓取成功率 0%，作品数据实际已停更 15–16 天，且全程零告警——功能处于「静默空转」状态。**

一句话讲：**调度没坏，取数全废，而且没人知道。**

---

## 二、实测证据

| # | 检查项 | 实测结果 | 判定 |
|---|---|---|---|
| 1 | 定时线程触发 | 今日 09:30 → 13:15 共 8+ 轮，间隔精确 30 分钟 | ✅ 正常 |
| 2 | 抖音进度 | `error`，`done=0/33`，「未采集到任何作品，请检查 Cookie 是否有效」 | ❌ 失败 |
| 3 | 小红书进度 | `error`，`done=0/1`，「bootstrap user/me failed: 登录已过期」 | ❌ 失败 |
| 4 | 抖音作品数据 | `_douyin_works.csv` 停在 **2026-09-02 13:58**（距今 359.7 小时） | ❌ 停更 15 天 |
| 5 | 小红书作品数据 | `_xhs_works.json` 停在 **2026-09-01 07:04**（距今 390.6 小时） | ❌ 停更 16 天 |
| 6 | 抖音失败根因 | HTTP 403，响应体 `Blocked by ArgusSecurityPlugin Uifid Not Found` | ❌ 风控拦截 |
| 7 | 运行环境 | `/opt/ecom/venv` Python 3.12.13，requests 2.34.2 / loguru 0.7.3 / qrcode 均可用 | ✅ 非依赖问题 |
| 8 | 账号配置 | 34 个（33 抖音 + 1 小红书），主页链接 33 个齐全 | ⚠️ 2 个重复抖音号 |
| 9 | 被删作品快照 | 快照 316/58 与实际文件 316/58 完全一致 | ✅ 当前一致 |

补充：后端 gunicorn 为 `--workers 1 --threads 8`，模块级 daemon 线程只启动一次，**不存在多 worker 重复触发问题**。

---

## 三、根因分析

### P0-1 抖音：浏览器指纹缺失 + 硬编码常量被 Argus 拦截（不是 cookie 缺字段，也不只是 IP）

关键反直觉点——**cookie 本身并不旧**：

- `douyin_cookie.txt` 9-16 18:30 更新，6119 字节、**65 个字段**；
- 内含 `UIFID`、`UIFID_TEMP`、`ttwid`、`s_v_web_id`、`odin_tt`、`sessionid`、`sid_tt` 等完整登录态字段；
- 但服务端仍返回 `Uifid Not Found`。

这说明 Argus 判定的不是「cookie 里有没有这个键」，而是**不认这台机器的指纹环境**。经第九节「本机住宅 IP 无 cookie 补测」修正后，按重要度重排：

1. **纯 `requests` 请求，无浏览器渲染上下文** —— ★ 第一道门。本机住宅 IP 不带 cookie 实测**同样 403、同样报 `Uifid Not Found`**，说明这一关与 IP 无关；
2. **脚本里 `WEBID` / `MS_TOKEN` / `VERIFY_FP` 是硬编码常量** —— 早期从浏览器复制的一次性值，早已失效；
3. **腾讯云 IDC 出口 IP** —— 第二道门。服务器即便带上有效 cookie 也过不去（与抖店链路同一个结论：IDC IP 信誉差）。

→ **单纯重新导出 cookie 解决不了**，这是环境问题；正确方向是「真实浏览器上下文 + 住宅 IP」，不能靠纯 `requests`。

### P0-2 小红书：登录态过期

`xhs_cookie.txt` 998 字节，`web_session` 存在但服务端判定过期。同属「住宅 IP + 真实浏览器 mint」问题。

### P0-3 全链路零告警（最该先修的一条）

- `_seeding_launch()` 用 `Popen` 拉起脚本后，**不检查退出码，也从不读进度文件的 `error` 状态**；
- 失败只写进进度文件，后端没有任何后续动作；
- 对比：抓取对账链路有 `fetch_reconcile.send_alert()` 钉钉告警，**种草链路完全没有接**；
- 日志每次 `open(..., 'w')` **覆盖**，历史失败轨迹全部丢失（现在只能看到最近一轮）；
- 前端只显示「数据更新时间」，用户无从判断数据是 15 天前的。

→ 结果是连续失败 15 天，没有任何人收到信号。

---

## 四、代码级缺陷清单

| # | 位置 | 问题 | 潜在后果 |
|---|---|---|---|
| 1 | `seeding_xhs.py:121` | 无条件写 `_xhs_works.json` | 账号全失败时写出空 `[]` → 后端 reconcile 把 **58 条作品全部判为「已删除」**（逻辑炸弹） |
| 2 | `app.py:_seeding_is_running` | 靠「30 分钟内 ts」判是否卡死 | 单账号抓取超 30 分钟会被重复触发；今日 09:36/09:40/09:42 已出现 3 次误拦提示 |
| 3 | `app.py:_seeding_progress_read` | 完全依赖脚本自己写进度 | 脚本启动即崩（如 `load_cookie()` 的 `sys.exit`）时进度永久停在 `running`，前端进度条无法自愈 |
| 4 | `app.py:/api/seeding/works` | reconcile 副作用写在 GET 里，读-改-写 230KB JSON | 前端每 3 秒轮询一次，反复写盘 + 并发写竞态 |
| 5 | `seeding_accounts.json` | 抖音号 `26450318184`、`27492706326` 各重复 2 次 | 同一账号抓两遍，浪费请求额度 |
| 6 | `.deploy/deploy.py` | 上传清单**不含 `tools/`** | 种草抓取脚本改动不会随部署上线，线上靠手工 sftp → 代码漂移风险 |
| 7 | `page-seeding.js:286` | 轮询上限 `160 × 3s = 8 分钟` | 33 账号顺序抓取易超时，前端提前报「未完成」，实际后台仍在跑 |

### 检查期间观测到的变化

13:47 实测「被删作品」为 **61 条**（9-15 的 4 条 + 9-16 的 57 条），13:55 复测已为 **0 条**。

- 判断这 61 条**高度可疑为误报**：数据文件 9-01/9-02 之后从未更新，而删除记录集中在两个时间点批量爆发——典型的「快照与不完整数据源首次对齐」造成的批量误判，而非作品真被删除。
- 若是在页面上手动清空的，这是正确处置。当前快照已与实际文件对齐，不会再新增误报。

---

## 五、修复方案

### P0（建议今天做，成本低、收益最高）

| 优先级 | 事项 | 说明 |
|---|---|---|
| ★★★ | 接入失败告警 | 复用钉钉推送链路，白名单只发李自豪；同一问题 6 小时节流 |
| ★★★ | 修 `seeding_xhs.py` 空数组写入 | 空结果不覆盖旧文件，避免污染「被删作品」 |
| ★★ | 日志改追加 + 轮转 | 保留历史失败轨迹，便于定位 |
| ★★ | 清理重复账号 | 避免重复抓取 |

### P1（取数能力本身）

抖音/小红书取数的方案对比：

| 方案 | 做法 | 成本 | 预期成功率 | 与现有体系一致性 |
|---|---|---|---|---|
| **A ★推荐** | 本机（住宅 IP）定时 mint 新 cookie 并上传，服务器复用 | 低 | 中高 | 与抖店「cookie 复用/保活」心法一致 |
| B | 服务器改 Playwright + xvfb + 真实 Chrome 渲染取数 | 高 | 中 | 与 `/opt/pw` 技术栈一致，但 IDC IP 仍是瓶颈 |
| C | 放弃服务端抓取，改本机定时抓完上传数据文件 | 中 | 高 | 需新增上传环节，与现有部署链路耦合 |

**建议：短期走 A（复用现有 `.deploy/ssx.py` 上传能力），若 A 仍被拦再切 C。**

---

## 六、P0 修复代码（可直接应用）

### 6.1 `tools/seeding_xhs.py` —— 修复空数组覆盖（替换原 121–129 行）

```python
        if all_rows:
            with open(OUTPUT_FILE, 'w', encoding='utf-8') as f:
                json.dump(all_rows, f, ensure_ascii=False, indent=2)
            msg = '共采集 %d 条作品' % len(all_rows)
            write_progress('done', len(accounts), len(accounts), msg)
            print('[xhs_batch] %s -> %s' % (msg, OUTPUT_FILE))
        else:
            # ★ 关键：抓不到数据时绝不覆盖旧文件，否则后端 reconcile 会把历史作品全判为「已删除」
            msg = '未采集到任何作品，已保留上次数据（请检查 Cookie 是否有效）'
            write_progress('error', 0, len(accounts), msg)
            print('[xhs_batch] ' + msg)
```

### 6.2 `backend/app.py` —— 新增告警能力 + 改造定时循环

在 `_seeding_launch()` 之后、`_seeding_auto_update_loop()` 之前插入：

```python
# ======================== 种草抓取失败告警 ========================
# ★ 与「数据分析日报」共用 dingtalk_push_users 表，这里只发白名单（当前＝李自豪），
#   绝不能靠改表 enabled 来限制，否则会连带改掉日报收件人。
_SEEDING_ALERT_ONLY_USER_IDS = ['17841605101729565']   # 李自豪
_SEEDING_ALERT_ONLY_NAMES = ['李自豪']
_SEEDING_STALE_HOURS = 3          # 数据超过 3 小时未更新即视为「定时抓取没产出」
_SEEDING_ALERT_INTERVAL = 6 * 3600  # 同一问题最多每 6 小时提醒一次


def _seeding_data_age_hours(platform):
    """作品数据文件的年龄（小时）；文件缺失返回 None"""
    f = _XHS_WORKS_FILE if platform == 'xhs' else _DOUYIN_WORKS_CSV
    if not os.path.exists(f):
        return None
    return (time.time() - os.path.getmtime(f)) / 3600.0


def _seeding_alert_throttled(key):
    """告警节流：同一 key 在 _SEEDING_ALERT_INTERVAL 内只放行一次"""
    path = os.path.join(_SEEDING_DIR, '_seeding_alert_state.json')
    st = {}
    if os.path.exists(path):
        try:
            with open(path, 'r', encoding='utf-8') as f:
                st = json.load(f) or {}
        except Exception:
            st = {}
    now = time.time()
    if now - float(st.get(key) or 0) < _SEEDING_ALERT_INTERVAL:
        return False
    st[key] = now
    try:
        tmp = path + '.tmp'
        with open(tmp, 'w', encoding='utf-8') as f:
            json.dump(st, f, ensure_ascii=False, indent=2)
        os.replace(tmp, path)
    except Exception:
        pass
    return True


def _seeding_send_alert(title, text):
    """把种草抓取告警推给白名单成员；永不抛异常，返回 'ok'/'nosend'/'error'"""
    try:
        if DingTalkClient is None:
            print('[种草][告警] 钉钉模块未加载，跳过')
            return 'nosend'
        cfg = _push_config()
        if not (cfg.get('app_secret') or '').strip():
            print('[种草][告警] 未配置 AppSecret，跳过')
            return 'nosend'
        rows = db_execute('SELECT id, name, user_id FROM dingtalk_push_users '
                          'WHERE enabled=1 ORDER BY id') or []

        def _in_whitelist(u):
            uid = (u.get('user_id') or '').strip()
            nm = (u.get('name') or '').strip()
            return bool((uid and uid in _SEEDING_ALERT_ONLY_USER_IDS)
                        or (nm and nm in _SEEDING_ALERT_ONLY_NAMES))

        users = [u for u in rows if _in_whitelist(u)]
        if not users:
            print('[种草][告警] 白名单内无可发送成员，跳过')
            return 'nosend'
        client = _push_client(cfg)
        sent = 0
        for u in users:
            uid = (u.get('user_id') or '').strip()
            if not uid:
                continue
            try:
                client.send_markdown([uid], title, text)
                sent += 1
            except Exception as e:
                print('[种草][告警] 发送失败(%s): %s' % (u.get('name'), e))
        return 'ok' if sent else 'error'
    except Exception as e:
        print('[种草][告警] 异常: %s' % e)
        return 'error'


def _seeding_selfcheck():
    """一轮抓取结束后的体检：进度 error / 数据陈旧 → 钉钉告警（带节流）"""
    bad = []
    for platform, label in (('douyin', '抖音'), ('xhs', '小红书')):
        p = _seeding_progress_read(platform)
        age = _seeding_data_age_hours(platform)
        st = p.get('status')
        # running 说明脚本还在跑，不判定；其余情况（error / idle / done）才看数据新鲜度
        if st == 'running':
            print('[种草][体检] %s 正在抓取中，跳过判定' % label)
            continue
        stale = (age is None) or (age > _SEEDING_STALE_HOURS)
        if st == 'error' or stale:
            bad.append((platform, label, p, age))

    for platform, label, p, age in bad:
        if not _seeding_alert_throttled('stale:%s' % platform):
            print('[种草][告警] %s 仍在节流窗口内，跳过' % label)
            continue
        title = '⚠️ 种草抓取异常：%s' % label
        body = ('### ⚠️ 种草监测中台 · %s 抓取未产出新数据\n\n'
                '- 抓取状态：%s（%s/%s）\n'
                '- 数据最新：%s\n'
                '- 脚本说明：%s\n\n'
                '**检查时间**：%s\n\n'
                '> 请打开「种草监测中台 → 数据更新」检查 Cookie / 登录态。') % (
            label, p.get('status'), p.get('done'), p.get('total'),
            ('%.1f 小时前' % age) if age is not None else '数据文件缺失',
            p.get('msg') or '无',
            datetime.now().strftime('%Y-%m-%d %H:%M'))
        r = _seeding_send_alert(title, body)
        print('[种草][告警] %s → %s' % (label, r))
```

改造后的定时循环（替换原 `_seeding_auto_update_loop` 整体）：

```python
def _seeding_auto_update_loop():
    """后台线程：每半小时自动触发一次作品抓取（抖音 + 小红书），随进程存活。

    - 启动后短暂等待，先做首次抓取，再进入每半小时一次的循环；
    - 任一平台仍在抓取（含卡死判定）时自动跳过，避免任务堆积；
    - 每轮触发后做一次体检：进度 error / 数据陈旧 → 钉钉告警（6 小时节流）。
    """
    time.sleep(10)  # 等服务完成初始化，避免与启动过程竞争
    while True:
        try:
            for platform in ('douyin', 'xhs'):
                err = _seeding_launch(platform)
                if err:
                    print('[种草][自动更新] %s: %s' % (platform, err))
                else:
                    print('[种草][自动更新] 已触发 %s 作品抓取（每 %d 分钟）'
                          % (platform, _SEEDING_AUTO_INTERVAL // 60))
                time.sleep(5)  # 两个平台错开，避免同时打满
            # 给脚本留出启动+失败的时间（实测抖音批量 403 约 12 秒即结束），再做体检
            time.sleep(120)
            _seeding_selfcheck()
        except Exception as e:
            print('[种草][自动更新] 触发异常: %s' % e)
        time.sleep(_SEEDING_AUTO_INTERVAL)
```

### 6.3 `backend/app.py` —— `_seeding_launch` 日志改追加 + 轮转（替换原 3905 行附近）

```python
    _seeding_progress_write(platform, 'running', 0, 0)
    log_path = os.path.join(_SEEDING_DIR, '_scrape_%s.log' % platform)
    try:
        # 超过 5MB 轮转一份，避免 append 无限增长
        if os.path.exists(log_path) and os.path.getsize(log_path) > 5 * 1024 * 1024:
            os.replace(log_path, log_path + '.1')
    except Exception:
        pass
    log_file = open(log_path, 'a', encoding='utf-8')
    try:
        log_file.write('\n===== %s 触发抓取 %s =====\n'
                       % (platform, datetime.now().strftime('%Y-%m-%d %H:%M:%S')))
        log_file.flush()
        subprocess.Popen(
            [_sys_scrape.executable, scraper],
            cwd=project_root,
            stdout=log_file,
            stderr=subprocess.STDOUT,
            creationflags=getattr(subprocess, 'CREATE_NO_WINDOW', 0),
        )
    finally:
        log_file.close()
    return None
```

### 6.4 上线注意

`.deploy/deploy.py` **不含 `tools/`**，`seeding_xhs.py` 改完必须手工上传：

```bash
python .deploy/ssx.py up tools/seeding_xhs.py /opt/ecom/tools/seeding_xhs.py
python .deploy/deploy.py        # app.py 走正常部署
systemctl restart ecom          # 让定时线程加载新逻辑
```

---

## 七、P1 改造建议（暂不动手，供决策）

1. **`_seeding_is_running` 改为进程级判定**：`Popen` 返回对象记下 `pid` 写入 `_seeding_pid_<platform>.txt`，用 `os.kill(pid, 0)` 判存活，替代「30 分钟 ts 窗口」这种粗判。同时脚本内**每页/每账号都刷 `ts`**（当前抖音仅在账号循环末尾刷，单账号分页久会踩线）。
2. **reconcile 从 GET 里移出**：改由抓取成功后的收尾钩子触发（或加 `_seeding_reconciling` 互斥锁），避免每 3 秒写 230KB JSON。
3. **前端轮询上限**：`maxTries` 从 160（8 分钟）提到 600（30 分钟），或改为「只要进度是 running 就继续轮询」。
4. **`deploy.py` 补 `tools/` 白名单**：至少把 `douyin_video_scraper.py` / `seeding_xhs.py` / `xhs_crawler.py` 纳入上传清单，消除代码漂移。

---

## 八、复检方式

```bash
python .deploy/ssx.py up .deploy/_seed_check.py /tmp/_seed_check.py
python .deploy/ssx.py run "python3 /tmp/_seed_check.py"
```

脚本输出 8 个段落（环境 / 账号 / 进度 / 数据新鲜度 / 快照 / cron / 触发记录 / Cookie 探测），**核心看第 4 段「数据新鲜度」——只要 `_douyin_works.csv`、`_xhs_works.json` 的「距今」超过 0.5 小时，就说明定时抓取没在产出数据**。

---

## 九、追加实测：「不登录能不能取数」（修正 P0-1 归因）

**实验条件**：本机（住宅 IP）Windows，系统 Python 3.12，**完全不带任何 Cookie**，纯 `requests`。
探针脚本：`.deploy/_seed_nologin_probe.py`（可重复运行）。

| # | 检查项 | 实测结果 | 判读 |
|---|---|---|---|
| 1 | 抖音作品接口 `/aweme/v1/web/aweme/post/` | **HTTP 403** `Blocked by ArgusSecurityPlugin Uifid Not Found` | 纯 requests 无浏览器指纹 → 必被拦 |
| 2 | 抖音主页 HTML | HTTP 200，73 KB，但 `_ROUTER_DATA`/`aweme_list`/`awemeId` 全部 **0 次** | 空壳页，数据全靠 JS 异步拉接口 |
| 3 | 小红书作品接口 `edith.xiaohongshu.com` | **HTTP 406** `{"code":-1,"success":false}` | `x-s`/`x-t` 签名是**硬门槛**，与有无 Cookie 无关 |
| 4 | 小红书主页 HTML | HTTP 404（redId ≠ user_id），112 KB，`noteCard` **0 次** | 同为 SPA 空壳，HTML 内无作品数据 |

### 结论

1. **「人眼不登录能看」成立，「脚本不登录能抓」不成立**，两者差别在**浏览器帮你做了三件事**：① 自动生成匿名设备指纹（`ttwid` / `uifid` / `s_v_web_id`）；② 自动执行 JS 算出 `x-s` / `a_bogus` 签名；③ 带上正常浏览器的 UA / Referer / TLS 指纹。纯 `requests` 三者皆无，一眼被识别为机器人。
2. **首页 HTML 均不含数据** —— 两个平台都是 SPA 空壳，所以「用 requests 抓主页 HTML 再解析」这条路不存在。
3. **★ 对 P0-1 的修正**：原先推断「抖音 403 = 服务器 IDC IP 信誉差」。本机**住宅 IP** 无 cookie 一测，同样 403、同样 `Uifid Not Found` → 拦在第一关的是**浏览器指纹缺失**，不是 IP。IP 是第二关（服务器带有效 cookie 也过不去）。**两道门都要过，缺一不可。**

### 对修复方案的影响

| 平台 | 真正的门槛 | 修复难度 | 建议路径 |
|---|---|---|---|
| 小红书 | 签名（可本地算，Spider_XHS 已解决）+ 登录 Cookie 过期 | **易** | **重新扫码登录即可恢复**，今天就能修好 |
| 抖音 | 浏览器指纹 + 签名 + IP | **难** | 纯 `requests` 原理上走不通，须改用 **Playwright 真实浏览器 + 住宅 IP** 实测验证 |

→ 小红书先修（低成本、当天见效）；抖音按第七节方案 A 落地前，需先用 Playwright 在本机住宅 IP 做一次可行性验证，再决定是否上服务器。

---

## 十、小红书已恢复（14:07 完成）

**结果**：`_xhs_works.json` 从 `09-01 07:04` → **`09-17 14:07`，59 条作品**（16 天停更结束）。
服务器日志：`已获取 30 条作品…59 条` → `共采集 59 条作品` → 进度 `done 1/1`；被删作品 `0` 条。

**根因修正**：P0-2 原写「小红书＝登录态过期」。准确说法是 ——
09-16 那份 cookie **从一开始就是无效的游客会话**：`tools/xhs_login.py` 旧版用
「cookie 里有没有 `web_session` 这个键」判登录，而**匿名访客同样带 `web_session`**，
于是导出游客 Cookie 却打印「✅ 已登录」。

**正解（已验证，可复用）**：本机真实浏览器持久化 profile 登录 → 导出 → 上传。
```
本机：python tools/xhs_login.py          # 可见窗口，复用 tools/xhs_profile
      → tools/xhs_cookie.txt
上传：ssx.py up tools/xhs_cookie.txt /opt/ecom/tools/xhs_cookie.txt
验证：cd /opt/ecom/tools && /opt/ecom/venv/bin/python seeding_xhs.py
```
`seeding_xhs.py:49` 优先读 `xhs_cookie.txt`（其次 `cookie.txt`）。
本次实测：**本地 profile 里的登录态本来就有效**，是探测代码写错才判成未登录。

**⛔ 已排除的路径**：服务器免浏览器扫码（`xhs_crawler.py --qrcode`）**4 次全部失败** ——
平台接口最终返回 `success` 但 `data={}`，`web_session` 不被替换，`get_user_info` 恒 `guest=True`。
该接口不下发登录态，**这条路不要再来第 5 次**。

**本轮修掉的两个真 bug（`tools/xhs_login.py`）**：
1. 假阳性登录判定 → 改为读 `__INITIAL_STATE__.user`，要求 `guest === false` 且拿到 `userId`。
2. `page.evaluate` 的探针写成裸箭头函数字符串 `() => {...}` → 求值得到函数对象、序列化即 `undefined`
   → 探测恒空、已登录被判未登录。**必须写成立即执行 IIFE `(() => {...})()`**。
   附带：首页游客态没有「登录」按钮、`/login` 会重定向回 `/explore`，改为多入口触发 + 每步截图；
   浏览器内核缺失时自动回退系统 Chrome / Edge。

**上面这三条已在当天下午实施完毕并上线验证** —— 见第十一节。抖音取数（403）维持第九节结论，需另行验证。

---

## 十一、P0 已实施并上线（09-17 14:16–14:22）

改动前全部已备份到服务器 `/opt/ecom/.backup/bak_20260917_alert/`（app.py / seeding_xhs.py /
douyin_video_scraper.py），可整体回滚。

| 文件 | 改动 | 上线方式 |
|---|---|---|
| `tools/seeding_health.py` | **新增**：抓取体检 + 钉钉告警（约 300 行） | `ssx.py up` |
| `backend/app.py` | 定时循环接入体检、日志追加+轮转、`is_running` 看真实进程、被删对比加 guard、启动自愈 | `ssx.py up` + `systemctl restart ecom` |
| `tools/seeding_xhs.py` | 零作品不再覆盖数据文件；进度带 `ok/accounts`；退出码 3 | `ssx.py up` |
| `tools/douyin_video_scraper.py` | 进度带 `ok/accounts`；提前退出补写状态；按 `sec_user_id` 去重 | `ssx.py up` |
| `.deploy/deploy.py` | 上传清单补入 4 个 `tools/*.py`（以前 tools 全靠手工 sftp） | 本地 |

### 11.1 失败不再静默 —— 已实测送达

- 触发方式：定时线程**每轮先体检上一轮、再触发新一轮**（顺序不能反，触发会把进度覆写成 running）。
- 线上实测（`journalctl -u ecom`，14:18:41）：
```
[种草][体检] [体检] 2026-09-17 14:18:41 | 异常平台: 抖音
[种草][体检]     - 本轮抓取失败：未采集到任何作品（成功 0/33 个账号）
[种草][体检]     - 作品数据已停更 **360.3 小时**（最后更新 09-02 13:58）
[种草][体检] [体检] 触发告警（内容变化）
[种草][体检] [告警] 接收人白名单命中 1/4 人（跳过 3 人）
[种草][体检] [告警] 送达 1/1
```
- **判据**（任一命中即告警）：`status=error` / `ok<accounts`（部分失败）/ `running` 超 45 分钟
  （进程静默死掉）/ 作品数据距今 > 3 小时 / 数据文件不存在。**不看脚本退出码，只看有没有数据产出。**
- **去重**：同一异常 6 小时内只发一次（实测第二次运行输出「同一异常已提醒过，冷却中，本轮不重复打扰」）；
  内容变化立即发；异常消失时发一条「✅ 已恢复」。
- **接收人**：`tools/seeding_health.py` 内硬编码白名单（李自豪 `17841605101729565`），实测命中 1/4 ——
  与 `fetch_reconcile.py` 的 `ALERT_ONLY_*` 同口径，**没有动 `dingtalk_push_users` 表**
  （那张表和「每日数据分析日报」共用，改 enabled 会连带改掉日报收件人）。

### 11.2 空数组覆盖 + 「被删作品」误报

- `seeding_xhs.py`：0 条作品时**保留上次的数据文件**、进度记 `error`、退出码 3（原来无条件写 `[]`）。
- 后端新增 `_seeding_reconcile_guard()`：只有「本轮完整成功」（`status=done` 且 `ok==accounts`）才做
  被删作品对比。这是 09-15/09-16 那 61 条误报（09-15 的 4 条 + 09-16 的 57 条）的同源防线。

### 11.3 日志不再丢

`_scrape_<平台>.log` 由「每次 `'w'` 清空」改为**追加**，每轮写入分隔头 + 时间戳，
超过 2MB 自动滚动成 `.log.1`（只留 1 份历史），避免长年追加写满磁盘。

### 11.4 顺手修掉的 3 个坑（都是实测命中过的）

1. **服务重启必然误拦一轮**：旧判据「running 且时间戳在 30 分钟内」在重启后一定误判
   （本次实测到 xhs 被拦）。现在：抓到 `Popen` 就问进程是否还活着；启动时把残留的 running 重置为 idle。
   实测日志：`[种草] xhs: 上次抓取被服务重启中断，已重置进度` → 同轮 xhs 正常触发（14:18:47）。
2. **账号重复白抓一轮**：`李思宇-你来苹苹梨` 配了两条（同 `sec_user_id`）→ 现按 `sec_user_id` 去重，
   实测 33 → 32 个账号。⚠️ 另一组 `乔春涵-皱皱` / `乔春涵-蓝色天空` 虽然 `douyinId` 都是 `27492706326`，
   但**短链解析出的 `sec_user_id` 不同，是两个不同账号**（只是抖音号字段填错）——**不要删**。
3. **体检结论看不到**：gunicorn 下 stdout 是管道会被块缓冲 → 体检输出加 `flush=True`。

### 11.5 运维口径（以后只记这两条）

```
手动体检（只打印、不发消息）：
    /opt/ecom/venv/bin/python /opt/ecom/tools/seeding_health.py --dry-run
强制补发 / 清空去重状态：
    … seeding_health.py --force        … seeding_health.py --reset
```
后端接口：`GET /api/seeding/health`（只读、不发送、需登录；前端可用来显示健康状态）。

### 11.6 已知副作用 / 待办

1. 上线后抖音会**每 6 小时**推一条告警（403 未修）。不想被打扰时把
   `tools/seeding_health.py` 的 `SKIP_PLATFORMS = ['douyin']` 打开（体检照跑，只是不告警）。
2. 抖音账号列表里 `乔春涵-蓝色天空` 的「抖音号」字段是错的（复制了另一个账号的号），
   不影响抓取（抓取用主页链接），但建议在页面上改正。
3. 前端暂未接入 `/api/seeding/health`（本轮只做后端与推送）。
4. `.deploy/` 下积累了 80+ 个历史探针脚本（`_probe_*`、`_diag_*`、`_shot_*`），可择日清理。

---

## 十二、抖音取数已恢复（Playwright 方案落地，09-17 14:35–14:41）

**结果**：`_douyin_works.csv` 从 **09-02 13:58 → 09-17 14:40**（15 天停更结束），
**32 个账号 / 350 条作品 / 32 秒 / 退出码 0**。

### 12.1 定性修正：拦住我们的不是 IP

第九节曾推断「IDC IP 是第二道门」。本轮用三步只读探针在**服务器上**实测，**推翻了这个判断**：

| 场景 | 实测结果 |
|---|---|
| 真实浏览器 + 匿名（无 cookie） | HTTP 200、`status_code=0`、正常返回 `aweme_list` |
| 真实浏览器 + 注入 cookie | 同上，且能翻页取全量 |

服务器出口 IP 仍是 `119.45.187.154`（腾讯云机房）。**真正缺的只有两样：`a_bogus` 动态签名 + 浏览器指纹**，
而这两样都由真实浏览器自动提供。**不是 cookie 的问题，是「有没有浏览器」的问题。**

### 12.2 登录态的真实作用：解锁翻页

| 场景 | 李雅婷-辛福阿龙 | 高子涵-教你懂车 | 关键标志 |
|---|---|---|---|
| 匿名 | 18 条（第 2 页空） | 18 条（第 2 页空） | `not_login_module.guide_login_tip_exist=true`「看更多最新作品」 |
| 带 cookie | **21 条** | **25 条** | `has_more=0`、`not_login_module=None` |

→ **「每账号只拿到 18 条」= 登录态失效的明确症状**，直接作为判据使用。

### 12.3 改动清单（对外接口完全不变，前端 / 对账 / 体检均无需改动）

1. `tools/douyin_video_scraper.py` 重写为 Playwright 版（九列 CSV、同结构进度文件、退出码 0/3/1 全部保持）
2. `backend/app.py` `_seeding_launch()`：抖音改用 `/usr/bin/xvfb-run -a /opt/pw/venv/bin/python` 拉起
3. 脚本新增三类结局区分：**登录态失效**（不覆盖 CSV + 告警）/ **账号无作品**（沿用旧行 + 不告警）/ **真失败**（告警）
4. `load_accounts()` 增加 `platform=='douyin'` 过滤（修掉小红书账号被拿去解析抖音短链的噪音）

### 12.4 已知账号状态：3 个账号当前无公开作品

`李雅婷-小鱼🐟`、`刘昱岑- 噗噗面包`、`乔春涵-熊二` —— 09-02 时分别有 17 / 2 / 2 条，
现在接口规范返回空（HTTP 200、`status_code=0`、`aweme_list=[]`、响应仅 216–228 字节），
且 `max_cursor` 仍带历史值 → **账号侧状态变化（作品被设私密 / 清空 / 注销），不是抓取问题**。

现策略：识别为「无公开作品」→ **不算失败、不告警、沿用其历史行**（避免误判成「作品被删」）。
**建议确认这 3 个账号是否还在运营，废弃的从账号列表移除。**

### 12.5 运维口径

```bash
# 服务器手动跑一次
cd /opt/ecom/tools && xvfb-run -a /opt/pw/venv/bin/python douyin_video_scraper.py
# 本地调试（Windows，会弹可见浏览器窗口）
python tools/douyin_video_scraper.py --limit 3
# cookie 保鲜（方案甲）
python tools/douyin_login.py                      # 本机扫码
python .deploy/ssx.py up tools/douyin_cookie.txt /opt/ecom/tools/douyin_cookie.txt
```

**一句话判据**：进度文件 `msg` 里出现「无公开作品」= 正常；出现「登录态失效」= 去更新 cookie。

### 12.6 待办 / 未验证

1. cookie 保鲜的**方案乙**（服务器直接出二维码）未验证 —— 若可行可省掉本机这一步。
2. 3 个空账号的处理（见 12.4）待确认。
3. 11.6 的待办仍有效，但其中第 1 条「抖音每 6 小时推一条告警」已随本轮修复自然解除
   （本轮体检实际发出的是「异常已消失」的恢复通知）。
