# 2026-09-01

## 全量代码审查（只读，未修改任何文件）

对 `D:\代码\电商后台管理` 做了一次完整静态审查。规模：backend/app.py 5921 行（94 路由/73 唯一路径）、js/app.js 6168 行、index.html 2346 行、css/style.css 347 行、tools/ 爬虫集。系统已公网部署（nginx + gunicorn，域名 julangkeji.site）。

### 结论要点（供后续修复参考，按优先级）

P0 致命：
1. 后端 `_require_auth()`（app.py:1877）只验 token 存在，完全不校验 permissions → 任意登录用户可越权调用全部 94 个 API，含 `/api/admin/accounts`（GET 返回明文密码）、POST 提权、删管理员、删库表。
2. js/app.js:299 硬编码 `admin/admin123`，`getDefaultAdmins()` 另有 liyunying/123456、wangcaiwu/123456。
3. js/app.js:461-479 后端不可用时降级 localStorage 明文比对 → 认证可绕过。
4. 密码全程明文：DB 明文存、`WHERE password=%s` 明文比对、`admin_list()` 回传前端、localStorage 存 admin_pass。
5. `daily_analysis_reports` 建表只在 `if __name__=='__main__'`（app.py:5869），gunicorn 下不执行 → 每日数据分析四个接口线上必然报错。
6. DeepSeek key 明文硬编码于 backend/config.py 与 .deploy/server_config.py，且两者同 key（"隔离"未生效）。

P1 严重：
7. 连接池 5 vs gunicorn threads 8；`pool.get()` 第 97 行超时抛 queue.Empty 未捕获 → 500；第 103 行 `_create_conn()` 绕过 `_count<_size` 检查 → 连接泄漏。
8. `autocommit: True` 使所有事务失效，Excel 导入失败无法回滚。
9. 员工花名册返回身份证/银行卡/薪资/手机号，无脱敏无权限 → 《个人信息保护法》合规风险。
10. 违规词 `match_type='pattern'` 直接用库内正则跑用户输入（app.py:3940），ReDoS 风险 + 每次请求全表扫规则无缓存。
11. 登录无限流无锁定；`CORS(app)` 全开放。

P2 质量：
12. 三份巨型单体文件无拆分；requirements.txt 缺 `requests`（顶层 import，缺失服务起不来）与 `openpyxl`，与 .deploy/server_requirements.txt 不一致。
13. schema.sql 建 `ecommerce_admin`，config.py 默认连 `数据`，库名不一致。
14. 生产用 Tailwind CDN（官方声明仅供开发）；同时引入 Chart.js + ECharts 冗余。
15. 模块级 daemon 线程（app.py:5849）在 workers>1 时会重复触发抓取；子进程无并发锁；JSON 状态文件无文件锁。
16. `except Exception as e: return fail(str(e))` 回显内部信息；无 logging 模块，全靠 print；零测试。

### 已确认无问题的部分
- 未发现 SQL 注入：动态表名/列名全部走白名单（ALLOWED_TABLES / IMPORT_TABLES）或硬编码 meta 字典（_OD_PLATFORM_META）。
- config.py 未被 git 跟踪，git 全历史未泄露密钥。
- Cookie 已设 httponly + samesite=Lax；token 用 secrets.token_hex(32)；连接池有 ping 检测 + 退避重试；CSV 导入有多编码回退。

### 用户约束
本次审查为只读权限，未修改任何代码文件。

---

## 补充：前端技术栈核实 + 第二轮深入校验

用户指出"改代码没有前端框架"——核实确认，首轮报告未明确定位这层，补记。

### 前端栈（已实测确认）
- **零框架、零构建、零模块化**：无 package.json / vite / webpack / tsconfig；`import`/`export` 出现 0 次；script 标签非 module。
- 结构：两个 IIFE —— `ApiService`（app.js:7-289）、`App`（app.js:296-6168，巨型单体）。
- 渲染：字符串拼接 + `innerHTML`（119 处）；`textContent` 148 处；无虚拟 DOM、无模板引擎。
- 事件：index.html 内联调用，共 118 处（onclick 99 / onchange 15 / oninput 4），统一走 `App.xxx()` 前缀，无 window 挂载（classic script 顶层 const 处于全局词法环境，内联 handler 可直接访问）。
- 样式：Tailwind CDN（`cdn.tailwindcss.com`）+ 内联 tailwind.config；图表 Chart.js 4.4 + ECharts 5.5 均走 CDN。
- 结论：前端任何改动只能直接编辑巨型 app.js，**不能**按组件化/路由化思路重构；拆多文件需处理加载顺序与全局作用域，且 118 处内联调用是硬约束（改名即断链）。

### 修正首轮结论（下调）
- **XSS 风险下调**：`escapeHtml()`（app.js:1654）与 `esc()`（1659）确实存在，共 117 处调用。抽样 onclick 内插值仅涉及内部 `type` 常量，无用户数据裸插；属性统一双引号，未触发 escapeHtml 不转义单引号 `'` 的隐患（潜伏项，非现存漏洞）。

### 新发现的问题（第二轮）
- **真 bug**：index.html:1858/1864 内联调用 `App.vdToggleNotifications()` / `App.vdClearNotifications()`，但 app.js 中**完全未定义**，后端也无 notification 接口 → 点击"检测通知"铃铛必抛 is not a function。
- **app.js 含 1 个裸 NUL 字节**：偏移 192640，位于某正则字符类内（字面控制字符形式写 `/[\uFFFD\x00-\x1f\x7f-\x9f...]/g`）。导致 grep/git 将其判定为二进制文件，diff、冲突处理、自动化工具链会异常——**这也是首轮统计失真的原因**（需 `grep -a` 或 `-P` 才拿得到真实数据）。

### 改法约束（框架感知版）
1. 权限中间件 + bcrypt 密码哈希：纯后端，与前端栈无关，可直接落地。
2. 前端清理硬编码凭据 / 删除 localStorage 降级分支：需直接改 app.js 对应行，注意不破坏 IIFE 内部引用。
3. Tailwind 本地化 / 引入构建：属"要不要引入工具链"的架构决策，非小改，需单独评估工期与部署流程影响。
