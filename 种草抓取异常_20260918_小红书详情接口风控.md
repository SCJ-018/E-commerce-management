# 种草抓取异常处置报告 · 小红书「详情接口被风控」

- **报警时间**：2026-09-18 10:49（页面横幅 + 钉钉）
- **报警原文**：本轮抓取失败：小红书抓取失败：1 个账号重试后仍未取到作品（作品列表接口返回空，多为平台限流；连续两轮以上才需要重新导出 cookie），已保留上次的数据文件（进度 0/1，抓取时间 09-18 10:25）
- **处置时间**：2026-09-18 10:50 ~ 10:56（CST）
- **处置人**：李自豪 + WorkBuddy
- **结论一句话**：**不是 Cookie 失效，是详情接口被账号级风控（code 300011）。已中止无效重试并上线熔断，无需重导 Cookie。**

---

## 一、结论先行

| 判断项 | 结论 | 依据 |
|---|---|---|
| Cookie 是否失效 | ❌ **没有失效，不用重导** | `xhs_cookie.txt` 09:22:33 刚导出（TTL 14.3h）；09:49 那轮 59 条详情全成功，09:52 正常写入数据文件 |
| 登录态是否失效 | ❌ 没有 | 每轮 `resolve_user`（搜索小红书号）与 `bootstrap` 都通过 |
| 「作品列表返回空」 | ❌ **报警文案说错了** | 每一轮主页列表接口都正常返回 **59 条**（日志 `已获取 59 条作品`） |
| 真正根因 | ✅ **笔记详情接口 `get_note_info` 被账号级风控** | 返回 `{"msg": "账号异常，请稍后重试", "code": 300011}`，59 条全失败 → 整轮判 0 条 |
| 是否需要人工干预 | ⚠️ 通常不用（1~3 小时自愈）；若 6 小时仍不恢复再考虑换号 | 09-17 当天同类 300011 仅零星 8 条失败，本轮转为 100% |

---

## 二、实测证据（线上 /opt/ecom/tools）

### 2.1 轮次逐条复盘（`_scrape_xhs.log`）

| 轮次时间 | 列表接口 | 详情失败数 | 失败类型 | 结果 |
|---|---|---|---|---|
| 09-17 22:20 | 30 / 59 | 8 | KeyError `'success'`（=300011） | 部分成功 |
| 09-17 22:50 ~ 09-18 03:52 共 8 轮 | 正常 | **0** | — | ✅ 全部成功 |
| 09-18 04:22 | 0 / 30 / 59 | 59 | 58 条「登录已过期」 | ❌ 旧 Cookie 到期（09-17 14:15 + 14.3h ≈ 04:30，精准命中 TTL） |
| 09-18 09:22 | — | — | — | 人工重导 Cookie |
| 09-18 09:49 | 30 / 59 | **0** | — | ✅ 全部成功，09:52 写入 `_xhs_works.json`（59 条） |
| 09-18 10:19 | 0 / 30 / 59 / 30 / 59 | 118（2 遍 × 59） | KeyError `'success'`（=300011） | ❌ 风控 |
| 09-18 10:50 | 30 / 59 | 59×（进行中） | KeyError `'success'`（=300011） | ❌ 风控（已人工中止） |

### 2.2 关键量化

- 全天 300011 出现 **366 次**，其中 10:19 / 10:50 两轮占绝大多数。
- `xhs_pc_apis.py:499` 原文：`success, msg = res_json["success"], res_json["msg"]` —— 风控响应体里**没有 `success` 字段**，于是抛 `KeyError: 'success'`，真实原因（300011 账号异常）被 `_log_api_error` 吞成 `msg="'success'"`，日志里根本读不出是风控。
- 每轮失败后 `EMPTY_RETRIES=2` 还会重试，单轮无效请求 = **59 × 3 ≈ 177 次**，越打风控越久。

---

## 三、处置动作（已全部生效）

| # | 动作 | 文件 | 状态 |
|---|---|---|---|
| 1 | 中止仍在空转的第 10:50 轮（pid 2577049） | — | ✅ 已 kill |
| 2 | **暴露真实错误码**：宽容取值 + 把 code 带进 msg → `账号异常，请稍后重试(code=300011)` | `tools/Spider_XHS/apis/xhs_pc_apis.py` | ✅ 已上线 |
| 3 | **详情接口熔断**：连续 5 条风控即抛 `RiskControlError`，中止剩余条目（不再 ×3 重试） | `tools/xhs_crawler.py` | ✅ 已上线 |
| 4 | **失败文案分流**：风控 / 限流 / 登录态失效三种原因分别给处置建议 | `tools/seeding_xhs.py` | ✅ 已上线 |
| 5 | 登录态失效不再静默穿透 `SystemExit`（原来进度卡在 running） | `tools/seeding_xhs.py` | ✅ 已上线 |

> 三个文件都在 `/opt/ecom/tools/`，**每轮新起解释器 → 改完下一轮自动生效，无需重启 ecom**（不打扰全员）。

### 3.1 效果实测（10:54 手动验证）

```
[5/59] 详情接口连续 5 条被风控（账号异常，请稍后重试(code=300011)），中止本轮剩余 54 条，已成功 0 条
[xhs_batch] 小红书抓取失败：详情接口被平台风控（账号异常，请稍后重试(code=300011)）。
            主页作品列表与登录态都正常，本轮已提前中止以免加重风控，已保留上次的数据文件。
            通常 1~3 小时自愈，不用重新导出 Cookie；若连续 6 小时以上仍是该码，再考虑换账号或重导 Cookie。
EXIT=3
```

**无效请求 177 次/轮 → 5 次/轮（-97%）**，风控恢复窗口不再被自己拉长；`_xhs_works.json` 仍是 09:52 的数据（未被空数组覆盖）。

> ⚠️ **一个隐患**：`tools/Spider_XHS/` 被 `.gitignore:62` 忽略 → `xhs_pc_apis.py` 的
> 「暴露真实 code」这处修复**只存在于本机 + 线上**，不进版本库。若将来重新 clone/升级
> Spider_XHS，该修复会丢（熔断本身在 `xhs_crawler.py`，不受影响）。
> 该文件本地与线上 md5 已一致：`b9de7dd952071942d8809e759a185f50`。

---

## 四、待观察与后续建议

| ★ | 事项 | 说明 |
|---|---|---|
| ★★ | **11:19 看下一轮**（`grep 300011 /opt/ecom/tools/_scrape_xhs.log`） | 已恢复 → 详情有成功记录；仍是 300011 → 继续等 |
| ★★ | **12:52 会收到「数据停更 3 小时」钉钉告警** | 这是**正确**行为（数据确实停在 09:52），不是新故障 |
| ★ | 若 6 小时以上仍是 300011：先重导 Cookie（换 a1/web_session 可能重置风控标记） | 列表接口一直正常，所以重导只是「试一试」 |
| ★ | 再不行就考虑：轮次间隔 30→60 分钟、或详情只抓最近 30 条 | 间隔参数在 `backend/app.py` 的 `_SEEDING_AUTO_INTERVAL`，改动要重启 ecom（全员掉线），需单独约时间 |
| — | 本轮改动尚未提交 git | `tools/xhs_crawler.py`、`tools/seeding_xhs.py`、`tools/Spider_XHS/apis/xhs_pc_apis.py` 本地与线上 md5 已一致 |

---

## 五、经验沉淀

1. **小红书三种失败必须分开判**：登录态失效 → `resolve_user`/`bootstrap` 阶段就抛错；列表限流 → 列表返回 0 条；**详情风控 → 列表正常但详情 300011**（本次）。
2. **「列表能拿到」不等于「Cookie 有效」** —— 列表接口对游客也返回数据，只有详情接口能验证登录态与风控状态。
3. 风控类错误**不能重试**，重试只会延长风控窗口；必须熔断 + 保旧数据。
4. 报警文案要带原始 code，否则排查要多花 10 分钟去翻 366 条 KeyError 的 traceback。
