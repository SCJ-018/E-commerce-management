# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
*.spec.bak

# === 日志 ===
*.log

# === 敏感配置（含数据库密码 / API 密钥，勿提交）===
backend/config.py
.env
.env.*
!.env.example

# === 部署配置（含服务器 DB 密码 / API 密钥，勿提交）===
# ⚠️ 这两个文件已存在于既有提交中，仅靠本规则无法从历史里抹掉，
#    彻底清理需要改写历史（filter-repo / BFG）；密钥请另行轮换。
.deploy/stage/backend/config.py
.deploy/server_config.py

# === Claude 本地设置（机器相关）===
.claude/settings.local.json

# === PyInstaller 构建产物 ===
tools/import_tool/build/
tools/import_tool/dist/

# === 淘宝登录态（含 cookie，勿提交）===
tools/taobao_profile/
tools/taobao_cookie.txt
tools/_taobao_*.png
tools/_taobao_*.html
tools/_taobao_*.json
tools/_taobao_*.txt

# === 抖音采集（含 cookie，勿提交）===
tools/douyin_cookie.txt
tools/douyin_profile/
tools/_douyin_*.csv
tools/_douyin_*.json

# === 1688 采集（含 cookie，勿提交）===
tools/1688_cookie.txt
tools/cookies.json
tools/_1688_*.json
tools/1688_profile/

# === 小红书登录态（含 cookie，勿提交）===
tools/xhs_cookie.txt
tools/xhs_profile/
tools/cookie.txt
tools/_xhs_works.json

# === 第三方开源项目（从上游克隆，不纳入本仓库）===
tools/Spider_XHS/

# === 选品助手抓取（cookie 登录态 + 运行态产物，勿提交）===
tools/douyin_hot_cookie.txt
tools/aisou_cookie.txt
tools/aisou_localstorage.json
tools/aisou_profile/
tools/_aisou_*.json
tools/_tmall_*.json
tools/_hot_words_preview.txt
tools/_tmall_preview.txt

# === 恢复操作备份目录（临时，可删）===
_restore_backup_*/

# === 系统文件 ===
Thumbs.db
.DS_Store
desktop.ini

# === WorkBuddy 项目数据（本地笔记 / 记忆，不入库）===
.workbuddy/

# === 选品助手运行态任务文件 ===
tools/_selection_job.json

# === 部署时的数据库导出（体积大，勿提交）===
.deploy/db_dump.sql
.deploy/db_dump.sql.gz

# === 三平台店铺抓取（下划线开头 = 探测脚本/截图/运行产物/登录态 state，勿提交）===
# 登录态 state（含 cookie）在 _states/ 子目录，属敏感凭据
tools/qianniu_crawler/_*
tools/doudian_crawler/_*
tools/jd_crawler/_*

# === 品类映射运行产物 + 探测脚本（仅 mapper.py 入库）===
tools/category_mapper/output/
tools/category_mapper/_*

# === 服务器环境侦查脚本（一次性）===
.deploy/_probe_*.py

# === .deploy 本地调试产物（一次性探测脚本 / 截图 / 预览件，勿提交）===
.deploy/_*
.deploy/*.png
.deploy/*.pdf

# === tools 下一次性探测脚本与运行产物（下划线开头 = 临时，勿提交）===
tools/_*

# === 登录态 / cookie 备份（含凭据，勿提交）===
tools/*cookie*.bak*
tools/*.bak_*

# === 批量建号中间产物（含手机号 / 身份证 / 初始密码，勿提交）===
# 2026-09-18：accounts_plan.json / 花名册人员.json / 花名册导出.json /
# 账号初始名单_*.csv 里都是真实身份证与手机号，属个人敏感信息。
# 注意规则只作用于 .deploy 根目录，不影响 .deploy/stage/ 下已入库的文件。
.deploy/*.json
.deploy/*.csv
